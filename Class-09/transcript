Course Name: DevOps & Cloud [SWE]
Lecture Title: Kubernetes Fundamentals
Start Time: 01/09/2026, 11:00:00
Duration: 120 minutes
Generated by: kabeer.24bcs10119@sst.scaler.com
Model use: groq, whisper-large-v3-turbo
Downloaded via: Scaler++ Chrome Extension
Developer: Ritesh prajapati

==================================================

Thank you. Thank you. Thank you Thank you. So how to create a non-network? has a driver or not? Not right? That's why we call it as a non-network. So from non-network, are we able to connect with the Google or any kind of external websites or server? No. Right? Then, in a host network, what do you mean by host network Is it going to share my network of my local machine to the same docker network or not No No Why not So when I am doing hyphen p 8080 and 80 for Nginx okay so by default it is using which network? bridge network. Okay. And that time I have to redirect my AT port to AT-AT. Then and then I can see my Nginx webpage in a browser. But what about host network? It is actually sharing the network with my docker host. So I don't need to specify hyphen p, AT-AT and AT port. That's why we call it as a host network. It is sharing my local network with that network. That means I don't need to specify port number or port forwarding. So for a host network, I guess we run a Nginx webpage, right? 30% people are getting the Nginx webpage and for Mac user they are not. Correct? Right? Last time we checked that. So what was the command for host network, by the way? Let's suppose like Nginx application you want to start with a host network. Then how you run a container? docker run hyphen dit hyphen p or not? Not right? Then the name of the container hyphen hyphen name and how do you specify the network by the way? Hyphen hyphen network and host. And then Nginx. So after that I am able to see localhost AT with my Nginx default webpage Then we checked about docker volume What do you mean by docker volume How many type of docker volume we have Volume in storage Two type of volumes or storage we have inside a docker. First one is a default docker volume and second one is the bind mount. Okay? So what is the difference or you can say in which use cases we are using the bind mount? When we want to bound my current folder or any folder of my local machine to a container. That time I will use the bind mount. And docker volume is the default volume. But how do you specify a volume while running a container? Let's suppose like Nginx container I am running with the bridge network. how I'm specifying the volume name and the path. How I'm doing? Which flag? Hyphen V, then the volume name, colon and the path. Correct? So the volume name that we specified while running a container, that volume how do we create? What is the command to create a volume? Docker volume create and volume name. okay for network docker network create okay so can we communicate to container if by default I have a bridge network can to container can communicate inside one network okay but let's suppose I used a custom or user-defined bridge network and I put two containers in different different networks still I'm able to connect those containers? Okay, we are not able to do. And I gave you one homework to create a three network and then create one month container inside that network front and back in a DB. Have you tried that? Okay. So, yesterday I was telling the same thing to class, I mean, section number B. So, this is the documentation for all the tasks, I guess. This guy shared with me. Yeah. So we added all the assignment because he added like one person was telling me yesterday that he added the transcript, I guess. Right. Yeah. So we just modified those things. It will be easy for you to check like what kind of a task we have. So I guess four to five tasks we added as per every sessions that we had. OK. And for you guys, there is a link section A and section B. According to that you need to add your readme file link over there Okay So what do you need to do in a submission You need to create a DevOps heroes repo I guess you already have this repo right Inside that session wise, you need to pull all the changes that I added. Let's suppose today I added session number nine. Session nine, you can pull the resources in your own repo and whatever the homework I am giving to you, you need to add it as per the session specified there. Okay, for homework and one readme.md file is needed. So I'll give you one example. So in session number eight, we have a Docker Compose. Today we are going to learn the Docker Compose. Okay, then this is my application. Screen is not shared. Okay, share. Where we are Here See in each session last time I guess Nginx web application Node these web application I told you right So you created a folder inside one session folder So everything you need to organize like this. And then at the end, inside that folder, I need one readme.md file. Only one readme.md file. And inside that, whatever the task is there, just add one line description. this is the task then the screenshot of that output okay and then the second task and screenshot of that output and that link you need to copy and paste inside a form because I will show you let's suppose this is your repo here readme.md file is there. I'll copy this readme.md file path and the form. Okay, I don't know the form. This one This is your link section A opened it So here as per the session you need to add a link of readme.md file. Got it? This is your submission. Now let's start with Docker Compose. It's editable, if you added a wrong readme file, then you can edit another response. So today we are going to learn docker compose. But before starting with Docker compose, or maybe I need to check how many of you know the Docker file instructions. I'll ask you. like docker file instructions just let me know what does it mean by that instructions so first one is a from why we're using from for base image okay run command to run a command okay cmd triggering command of application at last of docker file okay then expose so what do you mean by expose it is actually exposing a port or it is telling us like your application is running on port number 80 or 8080 by default. What does it mean? It's actually exposing a port? No. No. Okay. Expose 80 in Nginx. That doesn't mean it is exporting in 80 port number in my browser. Just telling us your application is listening on port number 80. Got it? Why would why we need to use it. Do you know why this instructions we are adding in Dockerfile Because every time developer working with their code bases we are not as a DevOps engineer But we need to know they want to deploy a website we are building a CI CD pipeline. At that time, we need to know that this application is there, one component, backend component is there and it is running on 5000 code. And how do you, how I know that it is running on 5000 code. That's what for me, for DevOps engineer, person they are adding expose 5000 inside of the file so that's why I get to know I need to redirect traffic from 3000 or 5000 port to some another port in my web browser got it then a copy command copy from source to destination is copying the file is it a local command or a global command locally it will copy the file or I can also copy a file from directly link local command correct so which command we are using for copying the file from zip file rpm file or any link add command okay add command gives us extensive ability to copy plus a copy the external zip file or external resources got it what do you mean by entry point it's a main program in your docker container we cannot override or we can override we cannot override okay but cmd we can override got it okay and one last thing what do you mean by multi-stage docker file because multi-container or docker compose and multi-stage file is a bit confusing. Sometime after like five months or six months if you're not using Docker then it is confusing. What do you mean by multi Dockerfile and multi Dockerfile Or maybe multi Dockerfile which we call it as in Docker Compose Multi Dockerfile Two things I told you one is a builder stage and second one is a runtime or production stage In a builder stage how we mention it is a builder stage in the first line at last as builder. And in a runtime stage, how we are taking input from that builder stage, hyphen hyphen from equals to builder and the source path whatever we want to copy. Let's suppose nodejs application is there then dot this folder is there. Inside the best folder we have a index.js file which is my triggering application file right. So I have to copy the best folder and need to put it inside my runtime docker file stage. Got it? Multi stage is a a different thing. Multi-stage Dockerfile creates only one container. Make sure, make sure multi-stage from multi-stage Dockerfile we are creating multiple stages inside a Dockerfile but at the end it is going to create only one container. Okay, only one container and that's what we are running as in our website. But for Docker Compose, so let's say I don't want to create a three different network, I want to make it up my whole application. Frontend, backend, DB inside the network and I want to specify front end make sure not able to connect with database directly okay also I need to specify my networks so that's why we have to specify at least two to three network right that's why front end not able to connect with database directly that also one requirement then I need to specify volume volume maybe bind mount or maybe default docker volume with my database to save my user data. Okay. So how do I make it up everything at once? That is what Docker container, sorry, Docker compose means Docker compose is basically an YAML file Inside that you can write all the container specific instructions and once you are make sure you update file or you can say build that file just like we build Docker image right? Docker build that image that we build. Same here in Docker compose we are building three container, four container, five container, six container. Any container you want to build you can directly do it with the help of only one command docker compose up okay why we need to use docker compose you got idea why first thing yeah anyone why we need to use docker compose cause if you want to deploy one application a simplest application which has at least three containers front-end, backend and dp container. At least three container you have at starting. So how you are able to do it First you are creating a network then you are entering the front container inside that front network then you are making it up first thing second same thing you are doing with the backend same thing that you are doing with the database and now you need to add a database container to both the networks correct these many steps you want you need to do at least even a simple website you want to deploy with three containers but what if i tell you that we can do everything at once by just one file. I can manage each container up, down, scale down, everything by just one file. Okay, that is what Docker compose. So Docker compose means we are executing commands and we are, you can say, starting the multiple container at once. Okay, we call it as a multi-container file. Sometimes don't confuse with multi container and multi stage. Both are different. So how many of you know YAML YAML format YAML Okay there is a lot of websites are there games are there basically So you can just play with those game to learn the YAML YAML is almost around, you can say Python, if you know Python language, YAML is very easy. Intentation is there, array is there and dictionary is there. Nothing else. else. Okay. Key value and the array concept and dictionary concept. That's it. Okay. So first thing, how to write a Docker compose file? Just like we have a Docker file, same way we write a compose file, same name as an Docker hyphen compose dot YAML. Okay. Some old formats or some some old applications are using compost.yml, not the docker-compose.yml. Okay, but most of the application in this 2026, 2024, maybe four to five years, they make sure that the file name is docker-compose.yml. Okay, this is the file. This will be hard for you. Yeah, see, now I'm going to explain you how the docker compose actually works. So anything related to container I'm adding, so there are three components by the there. One is a network, one is a volume and then the database container. I'm sorry, only container. Okay. So whatever I'm specifying here, frontend. Frontend is the name of the container just like we are giving hyphen hyphen name while running a container. Same thing I have given a container name as a frontend. Okay. Backend is also a container name. Database is also a container name Okay But we don call it as a container name We call it as a service Okay So how to specify service So first tag which is services that means inside the stack I able to add how many services how many containers I want to add. So first container that we add, which is Nginx. Second one is also Nginx and third one is a MySQL. Okay. So we know that Nginx we are using for frontend And front end we need to expose to external traffic because I want to see my Eugenics application in my browser. So that means we need port. We need port for that. So the first thing that you need to mention which is service name. Inside services we have a service. So the first service which is front end. So in frontend service which type of image you want to deploy or you want to pull, you want to build. Either you have your code base frontend and you have your like docker file So you can directly use your docker file like this so copy this so frontend one let's suppose or you needs to build okay from frontend So see this. There are two types. Either you pull an image which is by default you have. So Nginx we know that from the docker registry, from the docker hub, we can pull the image. Correct? So I can directly mention image and the image name. Even I can mention the latest tag or whatever the tag like v1 tag is there. I can take the image from there. Okay? This is one way to specify image and then its port because it's a front end, we need to export it. Correct? So, how to do it? Port. The first is host port or which one Host port or container port Host port and second one is the container port Okay two things only we need for front end Two things we added but what if I have my own docker file? What if I don't want this Nginx image? I don't want to pull the Nginx image. I have my front end application. I have my complete code over there react application or maybe like node.js application. I have my frontend folder and inside that I have my code. So make sure you added also Dockerfile inside your frontend code base, right? So if you have your own Dockerfile and you need to build, you need to build your image out of that Dockerfile, then you can specify build instead of image. What do you need to specify? Build. And then the location of Dockerfile. Location of Dockerfile. So, I'll give you a demo. So inside this demo, there is a front-end folder inside that I have added index.html. Now if I see their docker component. file and here can you see backend is there from backend they are building a backend see inside the backend I have a docker file correct inside the backend folder so in my docker compose file I don't need to pull any image I have my code backend code and from the backend code I have a docker file inside my backend folder so I will tell that docker compose hey see you are here okay in the same location can you just check the backend folder inside that you will find a docker file so how to say inside your backend service build and dot forward slash and backend so it will automatically go to backend folder and then it will search for default docker file okay and And then it will build an image out of that docker file. This is advanced but yeah we see First we check with a simple normal docker compose file So if you have an nginx image or alpine image or any type of image you can do image and the image name and the port for front end you will always specify a port always okay and for back end you will never specify a port never because back end we can't expose have you ever seen like normally if I run this then I am able to see my Nginx default web vision on 8080 port. But let's suppose my backend application is there and Nginx or maybe another Alpine image or maybe another backend port is there. From there I build an image. So do I need to see that backend stuff directly on my web browser? We never see. Have you ever seen? Backend directly they're exposing all the APIs and you are able to If we are doing this stuff, that means any kind of database, user name, we are exposing to the internet. It is the same thing from the front end we are able to access the database or maybe from the back end we are able to access the database in my browser But if we are working with Microsoft, then we need to expose that APIs to other servers? Yeah, we need to expose, but not all the APIs. Only the APIs that we need to connect with our database. Those APIs only we are exposing. Like maybe you are telling like hyphen forward slash API and those lines, correct? Not the complete backend. We are never exposing our backend APIs in external traffic. Okay, even if a developer is developing and they're able to expose any API, that means it is their company laptop, their company's private network, only the organization people are able to access those back end APIs, not any outside person. That is the thing Okay And the last service that we specify like we call it as a database service Okay For that let suppose Postgres you are using or maybe MySQL using or maybe MongoDB or maybe another any kind of database you are using. So if you are pulling an image, you can pull it directly. Or if you are building your own from like image from your own Docker file, Then you need to specify build and then whatever the path of the Dockerfile. Okay, because it's a database. So we have a password, we have a username, correct? So to specify environment variable. So in Dockerfile, do you know that we specified environment variable by ENV, then node version, or maybe the node specific environment, node environment equals to development, right? That we did. Same way if you want to specify the username and password, we never specify password like this in actual production because we never use Docker Compose. It's like previously, before using Kubernetes. Kubernetes was not famous enough at that time. At that time we are going to use the Docker Compose. Okay? But ma, if you have to, how can you expose a limited number of APIs to the back end? That's a new code, right? But you have to expose the port to outside. Why we need to expose a back end port? So how do you connect or call APIs without code? No, no, we are able to connect. From frontend container, if you do exec command, you are able to connect with the backend. But not from the outside of network. I'll tell you, I have a demo. Okay. So how do you specify the environment variable? By environment and then the variable name and the value of that variable. Okay, and last thing which is volume. If you want to specify volume or maybe network then you can specify like this volumes and then volume name and the path Just the same way what we are doing by running a container Docker run hyphen did hyphen v volume name colon and the path Same thing we are specifying volumes, volume name and the path. This is a default path of MySQL. But if you are specifying volume here, here you are specifying the volume, make sure you are specifying at the end as in service. Can you see as in service you are specifying all the thing and then you need to specify parallely of this service volumes and also networks. If I have a network then I'll specify networks too. Okay, like this front end network and back end network. Then I can use this front end network and back end network here. Now I can use it. Network and then which is my backend network. What was the name Backend network Like this Okay. If you want to specify a network. But make sure network and volume you need to specify outside of the services. If you are using network, that means you need to specify network. If you are using volume, then you need to specify the volume. Same volume that you are using. Same volume name. And same network name that you are using. All the networks you can specify at once. All the volumes that you are using, it's specified here. Okay. It's a basic file that we created. What other thing we added? Back and front and here. to front in the end it here okay simple one let me run this let me first do a system prone Morning I cleared all the images Now where we are, where we are, session number 8, okay, which docker file? networking volume what is the name docker session number eight inside docker networking volume here we have a docker compose app we are inside a folder and now I want to build this complete containers. How will I do? docker compose of up, hyphen b, hyphen b for same just like we were doing in container hyphen b for detach mode yeah for running processes in background correct if i'm not doing then i'm able to see all the logs of all the three containers so docker compose up and i'll do up so it's pulling my sql image and then the nginx image i just took an example as an backend engine image which is not a standard you you need to use your code so we have one demo where we created front-end backend and database we make it up how you can not connect from front-end to database that we also show you a demo and how you can connect to backend uh from the front-end container only not even in the website you are not able to connect with the backend okay so Can you see a docker compose application why it's not working scroll down scroll down yeah see automatically if i do a docker compose then i can see docker compose app and then i'm able to see frontend hyphen one container backend hyphen one container and database hyphen one container the same name that we have here okay i did not added hyphen D Can you see I did not add it from here Okay see I haven specified hyphen D so I am able to see all the containers are running If I do a control C it will stop Something happen. I'll do a control C, stopped. All the containers are stopped. Now I'll run the same container by hyphen D. Are you able to see by the way? Oh my God. DevOps heroes, right? DevOps heroes, yes. Shession number eight and Docker compose app here, yes. Yeah, see we are in the same. Yeah. So now docker compose up hyphen d See all the three containers are started I can see by docker sorry type it. Docker compose ps. See, just like how to list a container by docker ps, correct? docker ps same thing to list docker compose container you can use docker-compose ps previously it was working now you can use like this also it's not working I need to call these people for mic issues. Can you do one thing? Can you write the same file, Duncan Composer? Hello, the mic is not on the back. Yes, the mic is coming. I am going to go to the ground floor. See, by Docker, Compose, PS command, you are able to see all the containers. Even the container which I run. If you have this little time it will get noise from this It will get noise from this noise it starting it starting but it coming now it's coming I don't know if you're coming it's closed on and off it's not coming it's not coming it's coming yeah I'm going to go. I'm going to go. Let's go. so Every instructor is out of the mic Who is out of the mic? They are out of the mic. That's why they are not working. Okay guys, how many of you run the same obligation? Are you able to see the three containers? How many of you are able to see the three containers? Let me know. Can you check the localhost AT You see the Nginx webpage We have one demo Let complete this demo and then we start with the Kubernetes component today Overview of Kubernetes and the component. See? Everyone able to see? Okay. Let's recap about what we did. Here. Docker compose means it's a multi-container application to run a multi-container at once. Okay. Now few more commands regarding Docker compose. So the first command that we are using which is Docker compose up hyphen d hyphen d for detach mode. Okay. If you are not using then if maybe clearing the session, then it will stop all the containers. Second, how to check all the containers at once just like docker ps, we can check with the docker compose ps. Third command, how to stop the containers by docker compose. Can you tell me? Docker compose down. Correct. okay docker compose down this is the command if i run this command then it will stop all the containers that were running now if i see again by docker ps or docker compose ps i'm not able to see any running container because i need it down okay if i want to up the container again then which Which command I will use? Docker compose. Docker compose. Which command? Up-d. And there is one more command which is Docker compose up What do you mean by hyphen hyphen build anyone knows maybe maybe have you checked or you know docker compose hyphen hyphen build it's actually yeah anyone It's actually building your Docker image layer again from a Docker file. Okay. So, I do see again, something is created Docker compose app default is created and all the containers that we store, those are started. Those are only started because we used directly images from Docker Hub. Let's suppose I have a Docker file inside like front end, front end folder, back end folder. Okay. At that time, if I'm running hyphen, hyphen build and I did any kind of a changes in my Docker file then it will build that image of the building that image to rebuild your image You are adding hyphen hyphen build okay i guess four to five commands only docker compose up hyphen d hyphen hyphen build to build command then stop command docker compose not stop docker compose down down is the command to stop all the containers okay this is all regarding the docker compose and we'll have one demo right now What is the folder? This is the folder I guess. Demo. So I'll walk you through the demo first. Can you see? There is one front-end folder, one back-end folder and Docker Compose YAML file. See? This is our demo. By the way, in session 8, are you able to see in Docker Heroes repo right now this demo? Or I need to push it? Are you able to see? Demo folder. I guess I pushed it Yeah you able to see it Okay See this is my full stack application You can see one Docker compose file is there Inside that what I mentioned don feel terrible a lot of lines and long long lines It very very easy Services then network and volume. These three things you know guys, it's starting with, we told you right, services, network and the volumes. The volume we created as in DB data, front end network and back network two network we created now we have a services inside that we have a front-end service for that we are using nginx latest image okay and both we mentioned 8080 and 80 volume we mentioned front-end indexed html so it will take this front-end index.html from this front-end folder It will copy this file and it will paste it here. Okay. And second one, it will copy the Nginx.conf file from this folder and paste it inside default.conf. I'll tell you why we did it. Do you know that bind word? last time we did similar way we created index.html file right so if you are pulling an nginx image there is a default path user share nginx html inside index.html is there already there with the default nginx page so what we are trying to do i don't want to see their default page i wants to see my page. So what I need to do, I just need to replace their default file with my new Nginx index.html file. So where I added that file index.html. This is my one page static website that we have and I want to put it inside my Nginx default web page. Correct? So how I'll do, I'll copy with the help of volume, just like bind mount we used here. We copied my index.html file and I going to replace with the default Nginx file Okay So last time we were able to see a normal default nginx web page and then i added something in my index file and then i was able to see that directly in running containers same concept we applied here same for in nginx confine and i added i replaced with default confine and the network that we are going to use which is front-end network now we need to make sure that frontend not able to connect with bb directly okay so in database i use the image as in mysql 8.0 which is a tag environment variable mysql root password and the database database name also be given as a demo and then we specify the volume because we need to store username and all data okay for that i specify the volume name and the path this is a default volume Okay both the concepts we covered First is a wind mount that we covered here And the second one volume name in the path which is our Docker default volume Okay so we created a volume and then the network that we wants to add my database which is a backend network inside a backend network there are two containers one is a db container and second one is the backend container so the where is my backend where is my backend yeah this one same for backend we added backend as in service name but we don't need to use any default image from the docker hub I want to build my own image so that that's why I need my own code correct inside my code I need my docker file correct so I created a backend folder inside app.py basically this is a Python application and then I added my docker file you know this now correct last time we implemented one Python application inside that you use a Python as in from a base image, then work directory requirement.txt that we're copying. Then we are installing all the dependencies from the requirement Then we are copying all the code Right now I have only one file so I just read an app other than i will put a dot and dot then i exposing this application 5000 port that doesn mean i'm actually exposing it's just telling that your backend is working or listening on the 5000 port exposing we can expose my hyphen p and whatever the port you want to expose okay last command which is a cmd or maybe entry point so cmd is the like a default one that we are using to trigger our application python app.py this is our docker file for python application python is my backend and frontend that i'm doing here everything i just put it here again i will tell you first we created network frontend network backend network and volume db data okay services we have a front end inside a front end. I have a Nginx image. What I want, what I'm trying to do is I want to replace that default Nginx web page to my website web page. Okay. So I created a bind amount volume and I mounted my index dot HTML file. which is inside a front-end folder and I replaced it with a basic default Nginx website page. Same thing for Nginx confile I did. Okay and I attached this front-end service to a front-end network. So that's why front-end not able to directly connect with the directly database. Okay The second one which is a backend service. And backend I have my own docker file. I have my own code. I don't need to use any image. So I use the build instead of image. So build.forward slash and backend. So it is going to the backend folder and then search for a docker file. If able to find the docker file then we will move forward with the docker compose build. Second thing I told you know backend almost like we have two type of network that we are adding a backend container. One is inside a database network and one is inside a frontend because backend will communicate with both the things. Right now we have only two networks, only two networks, but still I want to make a wall between DB and frontend. So DB and frontend, we created two different, different network and backend I added in both the networks. Can you see something else like depends on database, depends on database this database what do you mean anyone knows this concept depends on database so first correct absolutely correct see so whenever I will do docker compose up hyphen d it will create a front-end it will pull a nginx image as in front then it will see backend and then it will see it will search a keyword depends If it is there and something I mentioned inside it depends on then it will not create first backend one but it will check whether my database image or image is up and running If it is up and running then and then it will move forward with this creating a image for backend. Got it? Depends on means it will check first database is up and running. Whatever I mentioned here, whatever. If any other service like database one or front end or any service I will mention here, it will check first that service is up and running. Then and then it is going to create a container for database or for backend. Okay. Right now the concept here that we have which is this backend will tell us depends on database. So it will check my backend container is up and running. If it is up and running, then it is going to create a backend container. Database is up and running then and then it will going to create a backend container. Okay. So now we need to run this application. What it is doing? Oh my God. Something is running already. I need to stop it docker compose yes yes it's right I need to make sure it will be down docker compose down yeah okay now let's go to the terminal ls I will do docker compose of hyphen d c it's doing can you do the same thing in your laptop how many of your following this is the last concept of docker only two anyone only two three four five tell me how many of you are following are nice almost almost your following take some time because three containers up and running Anyone have a question regarding this docker-compose file? You can tell me. This is the end of docker. If you able to understand Docker and troubleshoot all the container specific thing then Kubernetes will be very easy Okay In the base of Kubernetes we have container Kubernetes is nothing but it's a manager, managing all this container. Just like we are doing from Docker Compose, it's like boss of Docker Compose. So basically boss of Docker Compose is a Docker swarm. Have you heard Docker swarm? So when previously we don't have a Kubernetes kind of a thing. So that time we are going to use a Docker swarm cluster, which is Docker itself invented. So all the things that we are doing with the Docker Kubernetes, same thing we can do with Docker swarm. Docker swarm is one of the cluster that we used before Kubernetes. But there are specific problems with the Docker swarm regarding scaling, regarding rollout, all those problems were there. And that Docker Swarm we can only use for small applications, not for complex and large applications Because of those pros and cons the cuban it is invented by google and then they made it as an open source and then the services all the providers created EKS, AKS, GKE okay see containers are up and running can you see demo backend images their network they created two networks and and then the three containers. Now how to see this container by the way which command? How to list out all the containers? Docker compose. Can you see? Demo backend. Wait, wait, wait. We're in the same directory. One container is not awkward. What? Python Okay okay Yeah there were only two MySQL and the backend because Nginx is already running in 8080 port Let me rebuild again hyphen hyphen build hyphen d it not working yeah I forgot to mention hyphen hyphen build hyphen B yes okay I need to make sure it is down get down then I'll make sure it will be up so up hyphen d so it will be up I wants to see the front-end container that I was not able to see previously docker compose ps I'm not sure why why we're not able to get it yeah we our Our container is running by the way, 8080 port number. Maybe that's why. uh demo talker ps 80 where where our container is running 80 80 port ah our nginx was running okay okay we need to stop this container first where is the container no it's not running I don't know if I click here then I'm able to see if I click here then I'm not able to see it's a Mac problem hey guys are you able to see like your Nginx web page not the welcome engine is any other pages there you will just see no why I am not able to see yes somewhere this sport is running that why i suspect ah it running it not running Hmm, now it should run. From here, yeah. What was the issue then? Because it was running previously, then I made it up and run. Then I should get an error that port is already in use, right? I'm not getting even error. Very weird error. Okay. Let me check docker compose ps. What happened? Where we are Some another directory I did or what Yeah I able to see And in my VS Code, I don't know which kind of issue we are facing. In VS Code, I am not able to see the same thing. But it shouldn't be there. Change directory. To demo. Let me check here. Got it. inside repo you have to go and then you can only do the docker compose so so one thing I learned here here you learn the same thing what is the thing that we learned here anyone inside the folder you can only use the docker compose means like if I'm running the same command outside the my folder I'm not able to see even containers but for a docker commands that we are doing like docker ps, docker images, docker container. It will run on any terminal and it will tell you which container is running correct But for docker compose we cannot do the same thing Okay So this is our application that we deployed First we used Nginx and we replaced default Nginx web page with my simple web application and one button I just implemented. So it will give you the data from your back-end whatever we have. So now let's try to exit inside this container which is a front-end container and let me check our backend I'm able to access or not so backend is running on 5000 board sorry it's another application I was testing yeah see five thousand five thousand my application is running for backend but I'm not able to access it but I need to check whether from my front-end container I'm able to access the same or not so what we are going to do we are doing exec inside this container yes this is my friend then backend sorry this is my frontend container i'll do docker exec hyphen it container name bin bash okay we are inside a container let me check ping will not work because it's nginx image last time also ping was not working call should work call yes call is there so call then HTTP okay okay I'll tell you one more thing please my backend object by yes the nginx nginx See, do you know the Nginx reverse proxy? So here we added, you are telling my exposing the APIs. So these way we can mention for proxy pass setting. Okay so normally I redirected my local host backend thing to this 5000 API So if I want to connect with my backend call this API or even not even the API just backend service I want to connect, then I can do. Can you see hello from backend? Okay, forgive me for not adding slash end. Can you able to see my backend is working? So in real use cases, I'm not telling you that in real use cases, we are not doing like this, hello from backend. We have a health check. Anyone like implemented health check? We have a specific path backend 5000 then forward slash API and health check. So it will give you a string of health check, your DBs up and running. In actual application microservices, you're going to implement that. It's a simple application, static one, where we added just one functionality how to get a data from database to backend to our frontend application And you are not able to connect with frontend to db or maybe directly you cannot able to access your backend or backend any apis so i'm able to connect my backend application from here now one of the api that we created what was that forward slash an api so if i do like this i'm able to see a json file That's what the developers are exposing their APIs over the internet. That's what he was telling. Like if he wants to expose those things, then we cannot expose it directly from our browser. Even if I copy this, let me copy and then we'll go to browser. Localhost 5000 is not working. Let's suppose like back in 5000 is working. So suppose even if API I am removing, got the point? Isn this very subject due to when to export the port and back on What in the Towns of The way you say that we never export the backend port We never expose the backend port in Docker Compose surely. Because we are not using Docker Compose currently in this industry. We are using Kubernetes. Okay, so we never expose. yeah you can expose but we never expose actually in actual project we never do for testing we are doing but this is for like local simple setup for your own simple application not for even production not for even any cup no company and never ever most of the companies are not using this topic of course because we have a Kubernetes, we have a EKS. Why we should do something like this, all the containers, then I need to scale it up, scale it down. Why I need to do it when I have a EKS service? I'm not just trying to, like, you can't just go and go into the business. It comes with this small, operational... Yeah, that's right. We are not using this Docker Compose in current industry. It's for learning purpose and in each interview that I had given almost, I gave 100, maybe 150 plus interviews. Yeah, I mean, I failed almost and I learned from those. So I'm just telling you the main thing that they're going to ask you write a Docker compose file, write a Docker file, run this container, do this operation inside a container or in one interview, they almost interview almost interview that they're going to ask you write this Docker file, pull this image, write a Docker compose file, tell me regarding this Docker compose and these questions that they're going to ask you. That what I added this topic because it actually necessary and important topic Even though I not using it even they also know that we are not using docker compose in the industry we going to use the kubernetes cluster but before learning kubernetes cluster you need to make sure that you know the fundamental do you think like any company is not using kubernetes and using this docker compose Really? Which company? I myself run a company. Okay, so I'm telling you. So basically the decision to use Kubernetes or not, it depends on the scale. Like you just start up 47 users and run the Kubernetes cluster. Yeah, yeah, that is one good point, yes. If you have only one or two user and you are running the Kubernetes cluster, you call, like we call it as a duffer. even at a scale of 10 000 users or even 100 000 users or even a million users yeah running Kubernetes depends very much on the team size on the course the company is ready to accept what if why why i won just run ecs yeah you can use the ecs but these are like some of the pros and cons of using ecs over the cks so when even i started my learning i started with ec2 then ecs then ecr and then rarely i use eks because it's costing too much for learning yeah it depends on scale yeah we are not doing still i will be on the same point i know like maybe you are running a company that's a good thing very good thing i appreciate it but But any company when you grow up when you need to know that. We don't expose backend to public. Agree. But we never expose the port of backend to some another service I didn get you We didn expose our port to I think backend ports we never exposed My diagram looks something like there is a public subnet there is a private subnet there is a private subnet, backend backend, public subnet, backend backend, I don't have a subnet. I will never export the port of my backend from my private subnet to the internet. Yeah, yeah. Yeah. Yeah. That's what I'm saying that we can say, but when you learn some of the concepts, you will understand what I was telling you today. Yeah, you will learn it. Maybe two to three years it will take, but surely you will learn it that you're not exposing the code. Okay. But yeah, your points are correct for local projects or maybe a single project or maybe a hackathon project. Not for the organization. Okay, we'll... We saw our application. We exec inside this container. here. So how many of you are getting the same thing? Backend? How many of you are getting the same thing? Hello from backend while exit inside a container? Okay, good. So this is in your practice today for Docker. You need to run a same application that we run. You need to check you're able to access a big backend or from the front end or not. Same thing we did You need to check for database You able to do the same thing with database or not Okay This is in your homework You know the answer. I just need to check. Docker compose down. Free some resources and give me some CPU. Okay, guys. So, so far what we learned in Docker, Docker architecture, Docker container lifecycle, Docker basic command, Docker system prune commands, images, container, volume, network, type of volume, type of network, how to do containers or communicate with each other. how do you resolve a DNS by user-defined bridge network directly? How to create a user-defined bridge network? How can you make sure that your front or maybe in any application not able to connect with the DB directly Okay These things that we learned today And last one, multi-stage and multi-container environments. Multi-stage Docker file and multi-container, which is a Docker compost. Where we need to use, when we need to use, and why we need to use. these three things always you need to make sure for each DevOps concept just like he had a doubt so he asked like why in a backend we never expose of a backend so it's correct in one point that sometimes we need to test an API at the time we need to expose the port for that back end to test the API's but in real environment we never do after testing even we are never do okay Now we are moving forward with Kubernetes. So guys, you have any question for till Docker? We move forward to the Kubernetes We have half an hour We check all the components how it is working Overview of Kubernetes. Kubernetes architecture. Always refer to website. One is the actual website, official one. And if you're not able to understand then and then start with the Geek4Geeks or any like exam topics. I guess one was there, no? For learning HTML and Java, one website was the green color website. W3Schools. Yeah, that is also a good website. You can learn from there. But I will recommend to start with Kubernetes documentation. we'll get more clarity plus new updated content because Kubernetes itself handling these documentation, which is up to date. Okay. Before moving forward with Kubernetes, I want to tell you that Docker Swarm was one of the thing that we were using before Kubernetes. But there are some pros and cons inside a Docker Swarm. Just scaling is one of the problem. Second one, rollback inside a Swarm cluster. Third one, you can say when we have a complex deployment, legacy applications, at that some point of time, the Swarm cluster will not work as expected. Okay, these are the pros and cons for Docker Swarm cluster and Docker Swarm is a very, you can say, not costly. Like if you're comparing with Kubernetes, Swarm is very easy, easy to handle, easy to manage, easy to even learn. But if you talk about Kubernetes, it's a bit hard because for each component we have a server and for each component we have a quorum. Okay these topics we learn in next lecture But why we need Kubernetes Because Docker storm failed Yeah Main thing Docker spam failed That why something that we need and Google developers were on work and they thought like okay, let's create something for everyone. So they created a Kubernetes cluster to scale up millions or billions of nodes, worker nodes, to deploy your microservices, legacy application, not even just deploying, plus scaling, plus managing, plus even monitoring. Everything will be easy if you are using cluster, Kubernetes cluster, okay? So, do you know like we are running a container, we are deploying a website, simple inside a simple container, that's what we have inside a Docker. But if you have a lot of Docker container, like multi container, then we have a Docker compose. If you have a still I have a 10 plus or maybe 20 plus containers at that time we are using Docker swap And what if I have a millions or 10 of container and I have to manage all the container I need to scale down like maybe 300 content container for 40 plus services I need to scale up some containers for database backend or I need to scale down or make sure like some some team is working on a specific service they updated few things they need to roll roll back their existing infra. And they need to bring up a new updated version. At that time, this big scaling rollbacks, because of these type of requirements we had, we need to implement this Kubernetes. Kubernetes k8 is, k8s, okay? I guess we told you that funny fact, right? k8s. It letters are there, that's why we call it as a k8s. So, why we need Kubernetes? This is the reason that's why we need Kubernetes. Now how to use the Kubernetes and what is inside this Kubernetes Kubernetes cluster is there We call it as a key address Kubernetes or Kubernetes cluster So each cloud provider has its own service. For Amazon, which service? EKS. For Azure? AKS. And for Google? GKE. Don't tell me GKE. GKE. So, tell you the architecture. Same way what we learned in Docker, same approach. Docker architecture we learned, right? Docker architecture is a client server architecture. Here we have a Kubernetes architecture. Where we have two things, only two components. Two components. Just like client and server was there, right? Here we have a master and servant. Or master or worker. Okay, one is a boss, another are employees. Okay, one is a teacher, another are students. Okay, same way. We call it as a master, as a control plane, which is a boss. And second one, which we have a worker node or worker plane. There actually our application is running. Okay. So control plane or master node doesn't run any kind of application. First thing, control plane or master node, not running any application. It's just managing all the workloads, just managing all the workloads. And inside your worker node or slave node, or you can say the worker node, which we have here, we have nodes inside that particular environment. environment okay node means inside a node we have a pod and pod inside a pod we have a container means docker means you running your application containers inside this worker node okay two components one is a master one is a worker master also we call we call it as a control plane also okay that is the boss or you can say brain of Kubernetes brain if control plane will be crashed then you are your application you are not able to see so all I mean application may be up and running but you're not able to see all the functionality of working application this is an interview question we'll make sure we'll cover this in next lecture for specific interview or cases so basically we have a two component one is a master and one is a worker inside a master we have a four component how many four components you see here yeah see forget about this cloud controller forget it never existed Okay only four components are there One is the EPCD, second one is the API server, one is a scheduler and the controller. Four components. And do you know, in my fresher interview regarding DevOps engineer, they asked me, can you just explain how actually pod is creating and how these components are working together. So that means I need to tell them if I run a docker ps command, docker images command, what exactly happening in the backend. Same thing we have here Kubernetes, kubectl command where we have a docker command over the docker ps. Here we have a Kubernetes, kubectl get pods, kubectl get nodes, kubectl get all. So, kubectl is the CLI that we are going to download. So I need to tell them if I do the kubectl apply and deployment so it will read my deployment and it is going to deploy my application So What actually happening in the background when it is called the API server from the API server my call will go to the controller or scheduler From scheduler, my call will go to the worker node or master node. Inside the worker node, which components are there, it will interact with which first component, then how the networks are working inside that component, and then I'm able to see my container. These things they want in interview. And most of them do they're going to ask you like, can you explain me the component of Kubernetes? Or can you explain me the architecture of Kubernetes? Or can you explain me what do you mean by ETC, DQ, CTL, API server controller? What is the difference between these components? Any question they can ask you regarding architecture. It's a very, very important topic in Kubernetes. Okay. So we have a four components. Yeah. okay so whatever we have inside I mean a control plane and worker node means a boss and a servant okay they both have inside a one environment that environment we call it as in cluster EKS service is service when we are creating we are using the EKS service we are creating a cluster environment okay inside that environment we have a master node and the worker that is what cluster. Yeah, we have, we have. We can create multiple master. That's what we have inside a quorum. If you learn about quorum, so one master node will fail, then who will handle all the workload? Right? In industry, there are two to three master nodes, like one cluster that I'm working, they have a three master node. If one will fail, then another will take a workload and then it will check almost 7,200 something services are there inside that like worker nodes that we have So for that you need two three four five it basically an equation By that equation you can create a master nodes Okay. We have four component, etcd, cube api server, scheduler and controller. We call it as an etcd api server controller and scheduler see etcd is a key value storage database database it's a database where we are storing key value pair what we are exactly storing inside that database one thing kubernetes state state of a kubernetes each and every detail regarding your running container. If 10,000 containers are running, each container's detail are inside ETCD. Key value pair only key value pair Okay You can see like ETCD is storage or main storage of complete cluster Whatever inside a storage is happening inside a cluster is happening everything will be there in ETCD. APA server will ask ETCD, okay, which part I need to, this part I need to schedule. I will tell the scheduler that I need to add this service inside my worker node. you tell me what is the requirement for that service so it will tell like okay this service needs a 10 nmb of cpu and this this much of memory or like eight core of cpu all the things that they need api server will ask to etcd it will take all the input then it will tell the scheduler hey scheduler this is the service this is the requirement can you schedule in the required worker node. So then scheduler will come into the picture. Scheduler will tell the worker node okay these many servers are there Worker node is nothing but servers Okay these many servers are there in my worker node so scheduler will check which worker node i need to schedule this new service how it will get to know by the way by checking the resources let's suppose inside one service or inside one server already 100 service are running right now you don't have any uh like memorial resources enough resources for a new service then it will check in another server, right? We call it as a node. Another node it will check. Inside that node it will check. Okay. Do you have this much of memory, CPU and required resources for a new service? If it will tell yes, then scheduler will confirm yes. This service will deploy in that particular node. Got it? Scheduler's work is to get information from API server to schedule a particular pod inside a node. Let me tell you this terminology first. Pod, node, container and this component. I need an iPad. Okay. I will tell you visually directly. So container, you know, guys, container, you know, what do you mean by container? So we have this container, okay? And we have a pod. Pod means inside a pod, you are running a container. Pod is the smallest unit inside a Kubernetes cluster. Smallest unit. This is also like a way around question, okay? which is the smallest unit inside the Kubernetes cluster, which is a pod. So inside a pod, you are running a container. Maybe one container, maybe two containers, maybe three containers. You are running a container inside a pod. And in actual industry, we are running only one container. Only one container and one sidecar container for log purpose and other purpose, unit container. So in a pod, we have a container. Yes? Is pod like a pod is like you can say one container one big container one big environment inside that pod you have containers got it one big box inside that box you have your containers are running got it that is what pod and now we have a very big container where we are adding pods got it i guess i have one image that will clarify you do i need hmm okay see this is a good image maybe this one not this one not this one this is good for you See this is your cluster Inside a cluster you have nodes invoker nodes Inside a node you have a pod something like this Inside a pod you have a cluster sorry containers Got it? Node is the main thing where scheduler will connect with the node. Scheduler will tell, okay in this node, in this server I need to deploy my new application. So, you mean like if this container will be down then it will automatically be up. Okay, if you are adding volume then surely it will be there. So there is one more concept you can you can imagine there's one more boxes here which we have a volume if it is your database container then she right if database container is there and the database container may be spilled down maybe crashed or maybe deleted maybe stop and what at that time It will going to create a new container but what about my data Data will be stored in volume Kubernetes volume is one more than simple. Just like Docker, we have a Docker volume. Kubernetes, we have a Kubernetes volume. Just like Docker, we have a Docker network. We have Kubernetes network policies. Okay, that we are also learning. It's a bit advanced concept networking thing. Maybe you heard about service smash. Very advanced topic of Kubernetes. If you know this thing now, I mean, any company will hire you. I mean, if you are doing some projects related to service smash and basic concept, it's very, very easy. I mean, they need a skillful person who knows the actual concept like this. Not only the concept, how to do it, because that's why you need, you have to create a product or maybe a project and then you can showcase to them. But service mass is like one of the headache that we faced in our company. Even because it's very, very tough to understand which cluster, which node and then you are able to do import forwarding and all this stuff. It took you like last time I was working with a Kubernetes administrator who is managing all this stuff. And one is a Kubernetes developer. Do you know the certification by the way? CK, CKD, CKS, KCNA. There are five certification. If you're doing this certification, you will be a kubastronaut. kubastronaut, like if you heard about it. And this is the costliest certification I ever seen. Just like a redhead certification we have. Around $300. $500. Oh my God. Okay. These many dollars we have to pay for one certification. But they give you a second chance. Two chances are there to clear that certification. In one year. Have you completed any certification? It too much costly and you can get 50 or 60 discount on that certification I tried one of my friends he passed all the certifications and he also a trainer So he has that one community and from that community, he was telling me, I will give you at least 80% of non-certifications. Then I thought like, okay, how many dollars I need to pay? So that much I have to reimburse from my company. And then company said, I will not. And then I said, okay, then I will not do certifications if you are not reimbursing my amount, right? Even though I have to pay around 30,000, 40,000 for just a certification. So my strategy is if you are getting it free, do it anyhow. Even midnight someone pinging you that this certification is free, you just do it. Just do it. Because you know after one hour, after two hour, maybe after one month, this certification will cost you around $200. So when I started Oracle certifications, there were six certifications. We just cleared overnight. in just a month because I know those six or seven certifications like Oracle Cloud Architect Cloud Engineer then one is Solution one is DevOps related stuff Those certifications cost me around 700 plus dollars I did it completely free. After one month, they from free, they made it like all the prices were there. They were free for a month and then I got to know and we just cleared it. So that's what when you get a chance, like don't think twice. If you're thinking then the chance will go on surely. Okay so this is regarding node. Inside the node we have a pod which is the smallest unit inside a Kubernetes cluster. Inside a pod we have a container. Maybe one container maybe two containers. In industry most of the time you have only one or two containers. One is your main container and second one is an init container. Init container means a sidecar. Sidecar for your actual container. Okay, what do you mean by sidecar? This is also one concept that we going to learn next lecture Today we are going to give you an overview of Kubernetes okay etc guys a database storing everything and one more thing maybe you will not learn this directly but whenever you are writing deployment files yaml files for Kubernetes you are writing API version and kind. API version means everything inside a Kubernetes is an API object. So one of the good learning that after 20 years working on Kubernetes, people don't know. Okay. Everything, whatever you're creating in Kubernetes, it's an API object. Okay. So API server. So this is the front door of your Kubernetes cluster. If you want to enter inside a Kubernetes cluster, you need to speak with API server. If API server will allow you then and then you are able to deploy any kind of application. It's a front door. Okay. And you can say like a door. We have two doors, but what if like we have only one door? If you want to enter inside this classroom, you have to come towards that door. You need to open that door and then and then you can enter into classroom. Okay. Same way we have a cube API server. ETCD is a storage database that we're using scheduler. schedulers main job is to schedule a pod inside a node okay a pod inside a node this is our node this is our small small pods and these components will talk so that's how we are using scheduler last one is a controller okay before telling you controller let me explain you the nodes so you're able to get to know what do you mean by controller. So inside a node you have two components, Kubelet and Kubeproxy So Kubelet is the main component inside your worker node Kubelet will check with the controller that okay how many pods I need to make it up and run. So basically, Kubelet's task is simple. If I tell that I want three container for frontend, it will make sure three containers will be there. If one is down, again, it will create. If two containers are down, again, it will create. four containers are then it will remove the fourth container. Its only task is to check your container is up and running. It's running then send the heartbeat to the API server. Okay, it's up and running and three container I have to do. Tell the controller. That's it. Okay, queue let then queue proxy. It's not a complete component you can see. It's It's basically a network policies that we are enforcing in my pods. So can you see this pods? So we have particular network policies regarding network anything you want to add or delete or anything you want to update in network you going to use a QPROXY QPROXY is the manager in network, network department. And this is the main thing which is pod. So both the components are managing my pods. Network policies I apply to the pods. Kubelet also keep up and running the container inside a pod. So what do you mean by pod? Pod is nothing but an environment handled by these two guys, kubeproxy and kubelet. Inside a pod, you are running a container. Got it? CRI, container runtime interface. Yeah. Yeah. Sure. We can. Why not? yeah you can mention specific pod i wants to mention We are not basically using Docker daemon or actual Docker as in runtime in Kubernetes We are using container d. Previously, when Kubernetes first time came, at the time we are using Docker inside each pod, container runtime interface, we are using Docker. But after that, Docker was not supporting enough in Kubernetes. So they made some changes, custom changes inside Docker daemon and then they created container D. We call it as container D and this container D is the runtime interface. So you can imagine it's same as in docker everything is same this name is different with some custom changes. Okay basically we are running a container. Correct? So this is regarding our architecture. We have two components in our architecture. One is a master and one is a worker. Inside a master, we have a four component. See, cloud controller manager. I guess controller we did not complete. Controller we completed or not? Not, right? Okay. Why we need to use controller? So, So, Kubelet's task is to check your container is up and running or not. Okay. Then it will send containers state, which we call as a heartbeat of container. After five seconds, three seconds, four seconds, it will check the container. It's up and running, up and running. If it's up and running, it will give a heartbeat signal to API server. Then controller will check with the API server, like whether the pod is up and running or not. So we have a different, different type of controllers, like a node controller. So node controller, what it will do It will check like node is up and running Node is there or not Okay I have a four node I need a four nodes It is there or not That the controller work is Let say a replica set controller. Replica means how many pods you want inside a node. So this replica set controller will manage the replicas inside a node. Let's suppose like server one is the server node number one. Inside a node number one, I need only five container, five pods, five pods I want. Then replica set controller will make sure that in server one, in node one, I only need five pods, only five pods. Controllers is the component who is managing your worker node, your worker nodes component like the application deployment, then node port, I mean node, basically node information, which port you want to taint and toleration. This is bit advanced but when you get to know the commands you will get to understand what actually controller is doing Okay Four component ATCD database It will store the stage of Kubernetes and each pod container. Everything related to your cluster will be stored in ATCD which is a database key value pay database. Second one is the API server which is your front door of your Kubernetes cluster. You can't able to directly communicate with your application without entering from API server. Third thing which is a scheduler. Scheduler will schedule your pod inside a node. Task is only to check your service. Okay, these many resources you need. Okay, this is a server for you where you have the resources. You please schedule your service. You please add your deploy your service inside this node. That is what scheduler means. Schedule your code. Controller manager, we call it as in controller Controller we have a different type of controller like node controller is there replica set controller is there Regarding cluster also we have some controllers It will manage your worker nodes majority task Then we have a node, inside node, we have two components, kubelet and kubeproxy. kube proxy for networking stuff networking policies service mash or any kind of for networking you want to implement in force you can do it with the kube proxy okay by default we have a quick proxy and second one is a kube lid kube lid is the one component that will work with cube API server and controller controller and it will send the hard bit of a con sorry hard bit of a container to your API server. This container is up and running. Yes, share it with the API server. API server will tell the controller see this is up and running you wants to make sure you wants to down some service you wants to up some service then controller will tell yeah server need only three component. Can you just tell a kubelet? So again, the controller will tell the API server, okay, I need only three front end. Then kubelet will tell the kubelet, okay, I need only three front end container. If it is two, it will make it three. If it is four, make it three. Okay, got it? Pod is the smallest unit inside your Kubernetes cluster where we are running our actual container, our actual website. And CRI which is a runtime interface. Okay. We are using container D or maybe in some cluster they are using another environment too. But the default one which is container D. Okay. So that's it regarding Kubernetes. One thing that you need to do to practice Kubernetes from your side, a Minikube is required. You need to install minikube How many of you have minikube Maybe the service account Minikube anyone yeah okay i already added this link session 9 readme.md Kubernetes tutorial. Yeah. This is the link guys. For beginner. See. Create Kubernetes cluster. This one. Okay. Use minikube to create it. Directly they give minikube start and stop. Install minikube. You can install minikube. This is a minikube for Mac, for any operating system that you have. You can install it directly. After installing, you need to go to this. page. Let me bring this Minikube installation. Okay. From here, you need to install it. Second thing, what do you need to do in Homebook? Where is it? This is a two command, simple command. Minikube start, Minikube stop, Minikube status. Only two commands are given, but yeah, stop is also there. Just do the Minikube start. It will start something like container. Just like whatever we do in a docker compose we can see the container is up same thing will happen after running this command minikub start okay then check the status whether it's active or not okay and last thing that you need to make sure which is this documentation Make sure you read this documentation. Whatever we learned today regarding cluster architecture, Kubernetes architecture. Each component that we discuss is here. API server. If you want to deep dive in API server, you can just click on this API server You will get to know the history of API server and everything regarding that particular component etcd kubescheduler controller cloud controller manager it a controller manager basically we are using cloud service for that. node component that we have a kubelet, kubeproxy, okay, container runtime, CRI that we do. something new add-ons if anyone is more interested in DNS you need to read this documentation you do make sure architecture you know okay tomorrow well learn if I am running a command kubectl get pods and what actually happened in background. Okay. But for that you need to know each component. Okay. Thank you.